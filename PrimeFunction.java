/* Write a Program using method Boolean isPrime(int x) which checks if x is 
prime or not. The method returns true if x is prime else returns false.
Write main() to accept a number, pass it to the method and display the result
with proper message. */

import java.util.*;
public class PrimeFunction {
    boolean isPrime(int x) {
        int sum=0;
        for (int i = 0; i<a.length(); i++)
        {
            char ch=a.charAt(i);
            int c=(int)ch;
            sum = sum + c;
        }
        System.out.println("The sum is " + sum);
    }
    public static void main(String[] args){
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter the word");
        String k = sc.next();
        sum(k);
    }
}
